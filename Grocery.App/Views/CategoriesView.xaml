<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.CategoriesView"
             x:DataType="vm:CategoriesViewModel"
             Title="CategoriesView">

    <Shell.TitleView>
        <Grid>
            <Label Text="Categorieën" 
                   FontSize="Large" 
                   HorizontalOptions="Center" 
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Sectie voor het toevoegen van nieuwe categorieën -->
        <Border Grid.Row="0" 
                Stroke="#C49B33" 
                StrokeThickness="4" 
                Padding="10" 
                Margin="10">
            <VerticalStackLayout Spacing="10">
                <Label Text="Nieuwe categorie toevoegen" 
                       FontSize="18" 
                       FontAttributes="Bold"/>

                <Entry Placeholder="Categorienaam" 
                       Text="{Binding NewCategoryName}"
                       ReturnCommand="{Binding AddCategoryCommand}"/>

                <Button Text="Categorie toevoegen" 
                        Command="{Binding AddCategoryCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"/>

                <Label Text="{Binding ErrorMessage}" 
                       TextColor="Red" 
                       IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"/>
            </VerticalStackLayout>
        </Border>

        <!-- Lijst van categorieën -->
        <Border Grid.Row="1" 
                Stroke="#C49B33" 
                StrokeThickness="4" 
                Padding="10" 
                Margin="10" 
                HorizontalOptions="Center">
            <CollectionView ItemsSource="{Binding Categories}" 
                            Margin="20" 
                            HorizontalOptions="Center"
                            SelectionMode="Single"
                            SelectionChangedCommand="{Binding SelectCategoryCommand}"
                            SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Text="Categorie naam" 
                               FontAttributes="Bold" 
                               FontSize="16"/>
                    </Grid>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:Category">
                        <Grid Padding="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding Name}" 
                                   VerticalOptions="Center"
                                   FontSize="16"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <ContentView>
                        <VerticalStackLayout HorizontalOptions="Center" 
                                             VerticalOptions="Center"
                                             Spacing="10">
                            <Label Text="Er zijn nog geen categorieën" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   HorizontalTextAlignment="Center"/>
                            <Label Text="Voeg een categorie toe om te beginnen" 
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </ContentView>
                </CollectionView.EmptyView>
            </CollectionView>
        </Border>
    </Grid>
</ContentPage>
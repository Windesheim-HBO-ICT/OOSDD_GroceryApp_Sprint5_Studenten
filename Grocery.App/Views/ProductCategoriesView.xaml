<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.ProductCategoriesView"
             x:DataType="vm:ProductCategoriesViewModel"
             Title="ProductCategoriesView">

    <Shell.TitleView>
        <Grid>
            <Label Text="{Binding Category.Name}" 
                   FontSize="Large" 
                   HorizontalOptions="Center" 
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Zoekbalk -->
        <Border Grid.Row="0" 
                Stroke="#C49B33" 
                StrokeThickness="4" 
                Padding="10" 
                Margin="10">
            <SearchBar x:Name="searchBar" 
                       Placeholder="Zoek producten..." 
                       SearchCommand="{Binding PerformSearchCommand}" 
                       SearchCommandParameter="{Binding Text, Source={x:Reference searchBar}}"/>
        </Border>

        <!-- Producten tabel -->
        <Border Grid.Row="1" 
                Stroke="#C49B33" 
                StrokeThickness="4" 
                Padding="10" 
                Margin="10" 
                HorizontalOptions="Center">
            <CollectionView ItemsSource="{Binding FilteredProducts}" 
                            Margin="20" 
                            HorizontalOptions="Center">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Text="Naam van het product" 
                               Grid.Column="0" 
                               FontAttributes="Bold"/>
                        <Label Text="Voorraad" 
                               Grid.Column="1" 
                               HorizontalTextAlignment="End" 
                               FontAttributes="Bold"/>
                        <Label Text="THT datum" 
                               Grid.Column="2" 
                               HorizontalTextAlignment="End" 
                               FontAttributes="Bold"/>
                    </Grid>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:Product">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Label Grid.Column="0" 
                                   Text="{Binding Name}"/>

                            <Label Grid.Column="1" 
                                   Text="{Binding Stock, StringFormat='{}{0:F0} op voorraad'}" 
                                   HorizontalOptions="End">
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label" 
                                                 Binding="{Binding Stock}" 
                                                 Value="0">
                                        <Setter Property="Text" Value="niet op voorraad" />
                                        <Setter Property="TextColor" Value="Red" />
                                    </DataTrigger>
                                </Label.Triggers>
                            </Label>

                            <Label Grid.Column="2" 
                                   Text="{Binding ShelfLife}" 
                                   HorizontalOptions="End"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <ContentView>
                        <VerticalStackLayout HorizontalOptions="Center" 
                                             VerticalOptions="Center"
                                             Spacing="10">
                            <Label Text="Geen producten gevonden" 
                                   FontSize="18" 
                                   FontAttributes="Bold" 
                                   HorizontalTextAlignment="Center"/>
                            <Label Text="Probeer een andere zoekterm" 
                                   HorizontalTextAlignment="Center"
                                   IsVisible="{Binding Source={x:Reference searchBar}, Path=Text, Converter={StaticResource StringNotEmptyConverter}}"/>
                        </VerticalStackLayout>
                    </ContentView>
                </CollectionView.EmptyView>
            </CollectionView>
        </Border>
    </Grid>
</ContentPage>